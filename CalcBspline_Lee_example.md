# CalcBspline_Lee 算法原理与示例

本文档通过一个具体的 4 点例子，逐步梳理 Lee 算法为何能将插值方程组化为三对角矩阵。

---

## 1. 问题定义

**目标**：给定平面内 N 个折线端点，拟合一条通过所有点的光滑 B 样条曲线。

**输入示例**（N = 4 个点，2 维）：

```
P0 = (0, 0)
P1 = (1, 1)
P2 = (3, 1)
P3 = (4, 0)
```

**输出**：B 样条控制点 C0, C1, C2, C3, C4, C5（共 nCoeff = N+2 = 6 个）

---

## 2. 步骤一：1/4 幂弦长参数化

### 2.1 计算每段弦长

```
Δl₀₁ = ‖P1 - P0‖ = √(1² + 1²) = √2 ≈ 1.414
Δl₁₂ = ‖P2 - P1‖ = √(2² + 0²) = 2.000
Δl₂₃ = ‖P3 - P2‖ = √(1² + 1²) = √2 ≈ 1.414
```

### 2.2 取 1/4 幂（即对弦长开 4 次方根）

```matlab
du     = [Δl₀₁², Δl₁₂², Δl₂₃²] = [2, 4, 2]   % 弦长平方
du^0.25 = [2^0.25, 4^0.25, 2^0.25] ≈ [1.189, 1.414, 1.189]
```

### 2.3 累积求和并归一化

```
u_raw = cumsum([0, 1.189, 1.414, 1.189])
      = [0, 1.189, 2.603, 3.792]

u = u_raw / 3.792
  = [0,  0.314,  0.687,  1.000]
    ↑ u₀   u₁      u₂      u₃
```

> **为何用 1/4 幂？**  
> - 均匀参数化（幂=0）：忽略弦长差异，长段参数密集 → 振荡  
> - 弦长参数化（幂=1）：在曲率突变处过度拉伸 → 端点附近失真  
> - 1/4 幂（α=0.25）：折中，在等参数间距和等弦长间距之间平衡，经验上振荡最小

---

## 3. 步骤二：节点向量与 B 样条基

### 3.1 构造夹持节点向量

设阶次 p = 2（二次 B 样条），nMult = p-1 = 1：

```
knots = [zeros(1,1),  u,  ones(1,1)]
      = [0,  0,  0.314,  0.687,  1,  1]
         ↑ 重复   ← 内部节点 →     ↑ 重复
```

两端各重复 1 个节点（夹持端），保证曲线严格通过 P0 和 P3。

### 3.2 B 样条局部支撑性

B 样条基函数 Bⱼ,ₚ(u) 仅在 [tⱼ, tⱼ₊ₚ₊₁] 内非零，其余严格为 0。

以上面的节点向量为例，6 个基函数的支撑区间为：

```
B₀,₂(u): [t₀, t₃] = [0,    0.314]
B₁,₂(u): [t₁, t₄] = [0,    0.687]
B₂,₂(u): [t₂, t₅] = [0.314,  1  ]
B₃,₂(u): [t₃, t₆] = [0.687,  1  ]
B₄,₂(u): [t₄, t₇] = [1,      1  ]  ← 退化（端点处）
B₅,₂(u): [t₅, t₈] = [1,      1  ]  ← 退化（端点处）
```

> 注：实际控制点数由 `nCoeff = N+2 = 6` 控制，`bspline_eval_lee` 内部按此约束构造。

---

## 4. 步骤三：组装方程组 A·C = B

### 4.1 方程组含义

需要 nCoeff = 6 个方程求 6 个控制点，来源：

| 行号 | 约束类型 | 含义 |
|------|---------|------|
| 行 1 | 边界条件 DD0 | r''(u₀=0) = 0（起端曲率=0，自然边界） |
| 行 2 | 插值 BasisVal | r(u₀=0)  = P0 |
| 行 3 | 插值 BasisVal | r(u₁=0.314) = P1 |
| 行 4 | 插值 BasisVal | r(u₂=0.687) = P2 |
| 行 5 | 插值 BasisVal | r(u₃=1)  = P3 |
| 行 6 | 边界条件 DD1 | r''(u₃=1) = 0（末端曲率=0，自然边界） |

### 4.2 为何 BasisVal 是三对角稀疏的？

**关键性质：节点向量 = 参数值序列**

Lee 算法将参数点 uₖ 直接用作内部节点。这意味着在求值点 uₖ 处：

- uₖ 恰好落在节点区间 [tₖ₊₁, tₖ₊₂] 的左端点（因节点和求值点重合）
- 由局部支撑性，仅有紧邻该区间的 3 个基函数非零：  
  `Bₖ₋₁,ₚ(uₖ)`, `Bₖ,ₚ(uₖ)`, `Bₖ₊₁,ₚ(uₖ)`

结果：BasisVal 的第 k 行只有第 k-1、k、k+1 列非零 → **每行恰好 3 个非零项，分布在主对角线 ±1 的位置** → 三对角。

### 4.3 A 矩阵的稀疏结构（● = 非零，○ = 零）

**拼接后（swap_lines 之前）：**

```
      C₀  C₁  C₂  C₃  C₄  C₅
行1  [●   ●   ●   ○   ○   ○]  ← BasisValDD0（起端 r''=0）
行2  [●   ●   ●   ○   ○   ○]  ← BasisVal at u₀=0
行3  [○   ●   ●   ●   ○   ○]  ← BasisVal at u₁=0.314
行4  [○   ○   ●   ●   ●   ○]  ← BasisVal at u₂=0.687
行5  [○   ○   ○   ●   ●   ●]  ← BasisVal at u₃=1
行6  [○   ○   ○   ●   ●   ●]  ← BasisValDD1（末端 r''=0）
```

**问题**：行 1（DD0）和行 2（BasisVal at u₀）的非零位置完全相同，都在前 3 列。  
直接用于 Thomas 算法会失败，因为行 1 和行 2 不满足"每行非零项严格在相邻行 ±1 列"的三对角要求。

---

## 5. 步骤四：swap_lines 修复三对角结构

### 5.1 交换方案

```matlab
M([1, 2, end-1, end], :) = M([2, 1, end, end-1], :)
% 即：行1 ↔ 行2，行5 ↔ 行6
```

### 5.2 交换后的矩阵

```
      C₀  C₁  C₂  C₃  C₄  C₅
行1  [●   ●   ●   ○   ○   ○]  ← BasisVal at u₀=0      （原行2）
行2  [●   ●   ●   ○   ○   ○]  ← BasisValDD0             （原行1）
行3  [○   ●   ●   ●   ○   ○]  ← BasisVal at u₁=0.314
行4  [○   ○   ●   ●   ●   ○]  ← BasisVal at u₂=0.687
行5  [○   ○   ○   ●   ●   ●]  ← BasisValDD1             （原行6）
行6  [○   ○   ○   ●   ●   ●]  ← BasisVal at u₃=1        （原行5）
```

### 5.3 为何交换后满足三对角？

观察相邻行对的非零列范围：

```
行1 非零列：{1,2,3}
行2 非零列：{1,2,3}  → 与行1重叠 → 行2是行1的"子行"，共享主对角元素 ✓
行3 非零列：{2,3,4}  → 与行2重叠于列{2,3} → 相邻 ✓
行4 非零列：{3,4,5}  → 与行3重叠于列{3,4} → 相邻 ✓
行5 非零列：{4,5,6}  → 与行4重叠于列{4,5} → 相邻 ✓
行6 非零列：{4,5,6}  → 与行5重叠 → 行6是行5的"子行" ✓
```

每对相邻行之间恰好共享一个重叠列（非零交集），这正是**带状三对角矩阵**的定义：

```
主对角: [d₁  d₂  d₃  d₄  d₅  d₆]
下对角: [   l₂  l₃  l₄  l₅  l₆]
上对角: [u₁  u₂  u₃  u₄  u₅   ]
```

---

## 6. 步骤五：Thomas 算法 O(N) 求解

Thomas 算法是针对三对角矩阵的特化高斯消元，分两步：

### 6.1 前向消元（从上往下消去下对角）

```
对 k = 2, 3, ..., N：
    factor = lₖ / d_{k-1}
    dₖ     = dₖ - factor × u_{k-1}    ← 更新主对角
    bₖ     = bₖ - factor × b_{k-1}    ← 更新右端项
```

每步只涉及相邻两行，O(1) 操作 → 总计 O(N)。

### 6.2 回代（从下往上求控制点）

```
cₙ = bₙ / dₙ
对 k = N-1, N-2, ..., 1：
    cₖ = (bₖ - uₖ × c_{k+1}) / dₖ
```

每步 O(1) → 总计 O(N)。

### 6.3 对比标准 Gaussian 消元

| 方法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| Gaussian 消元 | O(N³) | O(N²) |
| Thomas 算法   | O(N)  | O(N)  |

对 N = 1000 段：Thomas 比 Gaussian 快约 **10⁶ 倍**。

---

## 7. 完整数据流总结

```
输入 N 个折线端点 [P₀, P₁, ..., P_{N-1}]
        ↓
1/4 幂弦长参数化 → u = [0, u₁, u₂, ..., 1]
        ↓
构造夹持节点向量 knots = [0^nMult, u, 1^nMult]
        ↓
bspline_eval_lee(Bl, nCoeff, u)
  → BasisVal    [N × nCoeff]   （插值约束，三对角稀疏）
  → BasisValDD0 [1 × nCoeff]   （起端曲率=0）
  → BasisValDD1 [1 × nCoeff]   （末端曲率=0）
        ↓
组装 A = [DD0; BasisVal; DD1]，B = [0; points.'; 0]
        ↓
swap_lines(A), swap_lines(B)  → 严格三对角矩阵
        ↓
extract_vectors(A) → v_m, v_l, v_u（三条对角带）
        ↓
tridiag(v_m, v_l, v_u, B)  O(N) Thomas 算法
  → 控制点矩阵 C [nCoeff × nAxis]
        ↓
constrSpline(C.', knots, Bl) → 输出 B 样条结构体
```

---

## 8. 关键结论

| 设计选择 | 原因 |
|---------|------|
| 1/4 幂弦长参数化 | 经验最优，减少端点附近振荡 |
| 节点向量 = 参数值 | 使局部支撑性恰好产生三对角结构 |
| nCoeff = N+2 | N 个插值 + 2 个自然边界条件，方程组恰好封闭 |
| swap_lines | 将边界条件行纳入三对角带，满足 Thomas 算法前提 |
| Thomas 算法 | 利用三对角稀疏性，O(N) 替代 O(N³) |
