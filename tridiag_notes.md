# tridiag — 三对角方程组求解器

本文档说明 `tridiag.m` 的数学原理、算法推导及使用方式。

---

## 1. 问题定义

求解 `n × n` 三对角线性方程组 `Ay = f`：

```
[ a(1)  c(1)                              ] [ y(1) ]   [ f(1) ]
[ b(2)  a(2)  c(2)                        ] [ y(2) ]   [ f(2) ]
[       b(3)  a(3)  c(3)                  ] [ y(3) ] = [ f(3) ]
[             ...   ...   ...             ] [ ...  ]   [ ...  ]
[                  b(n-1) a(n-1) c(n-1)   ] [y(n-1)]   [f(n-1)]
[                          b(n)   a(n)   ] [ y(n) ]   [ f(n) ]
```

**函数签名**

```matlab
y = tridiag( a, b, c, f )
```

| 参数 | 尺寸 | 含义 |
|------|------|------|
| `a`  | n×1  | 主对角线（全部使用）|
| `b`  | n×1  | 下次对角线，`b(1)` 不使用 |
| `c`  | n×1  | 上次对角线，`c(n)` 不使用 |
| `f`  | n×m  | 右端项，支持多列（同时求解 m 组） |
| `y`  | n×m  | 解向量（与 `f` 同尺寸）|

---

## 2. 算法：Thomas 算法（追赶法）

Thomas 算法是高斯消去在三对角矩阵上的特化实现，分为两个阶段。

### 2.1 前向消去（Forward Elimination）

**目标**：逐行消去下对角元素 $b_i$，将矩阵化为上三角形式。

对第 `i` 行（`i = 2, …, n`）：

**步骤 1**：计算上对角比值（消去系数）

$$v_{i-1} = \frac{c_{i-1}}{w_{i-1}}$$

**步骤 2**：更新主对角元素（LU 分解中 U 的对角线）

$$w_i = a_i - b_i \cdot v_{i-1}$$

**步骤 3**：更新右端项并求出当前分量

$$\hat{y}_i = \frac{f_i - b_i \cdot \hat{y}_{i-1}}{w_i}$$

> 初始条件：$w_1 = a_1$，$\hat{y}_1 = f_1 / a_1$（第一行无 $b$ 项，直接除以主对角元素）

---

**推导**：第 `i` 行原始方程为

$$b_i \cdot y_{i-1} + a_i \cdot y_i + c_i \cdot y_{i+1} = f_i$$

第 `i-1` 步消去后，第 `i-1` 行已化为

$$y_{i-1} + v_{i-1} \cdot y_i = \hat{y}_{i-1}$$

将第 `i` 行减去 $b_i$ 倍的上式，消去 $y_{i-1}$：

$$\underbrace{(a_i - b_i \cdot v_{i-1})}_{w_i} \cdot y_i + c_i \cdot y_{i+1} = \underbrace{f_i - b_i \cdot \hat{y}_{i-1}}_{\text{更新右端项}}$$

---

### 2.2 后向回代（Backward Substitution）

**目标**：利用已知的 $\hat{y}_n$ 向前回代，消去上对角元素 $v_j$。

对 `j = n-1, n-2, …, 1`：

$$y_j = \hat{y}_j - v_j \cdot y_{j+1}$$

---

## 3. 完整计算流程（以 n=4 为例）

设 $a = [4,4,4,4]$，$b = [*,1,1,1]$，$c = [3,3,3,*]$，$f = [f_1,f_2,f_3,f_4]^T$

**前向消去**

| 步 i | $v_{i-1}$ | $w_i$ | $\hat{y}_i$ |
|------|-----------|-------|-------------|
| 1 | — | $w_1 = 4$ | $\hat{y}_1 = f_1/4$ |
| 2 | $v_1 = 3/4$ | $w_2 = 4 - 1 \cdot 3/4 = 13/4$ | $\hat{y}_2 = (f_2 - \hat{y}_1) / (13/4)$ |
| 3 | $v_2 = 3/(13/4) = 12/13$ | $w_3 = 4 - 12/13 = 40/13$ | $\hat{y}_3 = (f_3 - \hat{y}_2) / (40/13)$ |
| 4 | $v_3 = 3/(40/13) = 39/40$ | $w_4 = 4 - 39/40 = 121/40$ | $\hat{y}_4 = (f_4 - \hat{y}_3) / (121/40)$ |

**后向回代**

$$y_4 = \hat{y}_4, \quad y_3 = \hat{y}_3 - v_3 \cdot y_4, \quad y_2 = \hat{y}_2 - v_2 \cdot y_3, \quad y_1 = \hat{y}_1 - v_1 \cdot y_2$$

---

## 4. 算法复杂度

| 指标 | Thomas 算法 | 一般高斯消去 |
|------|------------|------------|
| 时间复杂度 | **O(n)** | O(n³) |
| 空间复杂度 | O(n)（工作向量 v）| O(n²) |
| 适用范围 | 三对角矩阵 | 任意方阵 |

---

## 5. 适用条件

以下条件之一可保证算法不出现除以零的情况：

1. **严格对角占优**：$|a_i| > |b_i| + |c_i|$，对所有 $i$ 成立
2. **弱对角占优** + $c_i \neq 0$（对所有 $i$）+ $|b_n| < |a_n|$

---

## 6. 实现说明

### 6.1 MATLAB / 代码生成双路径

```matlab
if coder.target("MATLAB")
    y = tridiag_mex( a, b, c, f );   % MEX 加速版（MATLAB 环境）
else
    % Thomas 算法纯 MATLAB 实现（用于代码生成 / Simulink）
end
```

`%#codegen` 指令使函数支持 MATLAB Coder 生成 C 代码。

### 6.2 不覆盖输入

算法引入工作向量 `v`（n×1），不修改 `a`、`b`、`c`、`f`，对调用方透明。

### 6.3 多右端项支持

`f` 可以是 `n×m` 矩阵，`y(i,:)` 的写法使一次调用同时求解 m 组方程。

---

## 7. 使用示例

```matlab
n = 5;
a = 4 * ones(n, 1);   % 主对角线
b = ones(n, 1);        % 下次对角线（b(1) 不使用）
c = 3 * ones(n, 1);   % 上次对角线（c(n) 不使用）
f = rand(n, 1);

y = tridiag(a, b, c, f);

% 验证：构造完整矩阵，检查残差
A = diag(a) + diag(b(2:end), -1) + diag(c(1:end-1), 1);
residual = A * y - f   % 应接近零向量
```

---

## 8. 与 CalcBspline_Lee 的关联

`tridiag` 是 `CalcBspline_Lee` 的核心求解器。Lee 算法将 B 样条插值问题化为三对角方程组后，调用 `tridiag` 完成控制点的计算。参见 [CalcBspline_Lee_example.md](CalcBspline_Lee_example.md)。
