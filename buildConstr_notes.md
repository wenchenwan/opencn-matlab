# buildConstr — LP 约束矩阵构建

本文档说明 `common/Feedopt/buildConstr.m` 的数学原理、关键变量的物理意义及约束组装过程。

---

## 1. 函数定位

`buildConstr` 负责将进给率优化问题的**几何约束**（速度上限、加速度上限、段间连续性）转化为标准 LP 格式：

```
minimize  −c'·x          （最大化 ∫ ud²(u) du ≈ 最小化加工时间）
subject to  A·x  ≤ b     （不等式约束：速度/加速度上限）
           Aeq·x = beq   （等式约束：窗口边界 + 段间连续性）
```

---

## 2. 决策变量

LP 的决策变量 `x ∈ R^(N × Nwindow)` 是每段曲线的 **B 样条系数向量**：

```
x(:, k) = 第 k 段的 N 个 B 样条系数 [c_1, c_2, ..., c_N]'

物理含义: ud²(u) = B(u) · x(:,k) = Σ_j c_j · B_j(u)
          ↑ 参数速度的平方（注意：不是物理速度平方 v²）

v²(u) = |r'(u)|² · ud²(u)    （二者通过 |r'(u)| 联系）
```

用 `ud²` 而非 `v²` 作为决策变量，是因为加速度约束可以写成关于 `x` 的**线性**形式（见第 5 节）。

---

## 3. BasisVal 矩阵

### 3.1 结构

`BasisVal` 是 `[M × N]` 矩阵，**预先计算**（在 `initFeedoptPlan` 中），整个优化过程共用：

```
BasisVal(i, j) = B_j(u_vec(i))
                 ↑ 第 j 个 B 样条基函数在第 i 个采样点处的值

M = NDiscr   （u_vec 的采样点数）
N = degree + NBreak - 1  （B 样条系数/基函数个数）
```

### 3.2 可视化（degree=3, NBreak=5 为例，N=7, M=8）

```
每列是一个"帽形函数"（局部支撑，仅在相邻断点区间非零）:

B_j(u)
  1│  B1   B2   B3   B4   B5   B6   B7
   │  ╭╮   ╭╮   ╭╮   ╭╮   ╭╮   ╭╮   ╭╮
   │ ╱  ╲ ╱  ╲ ╱  ╲ ╱  ╲ ╱  ╲ ╱  ╲ ╱  ╲
  0└──────────────────────────────────── u
   0                                    1
```

矩阵数据示意（每行和 = 1，带状稀疏）：

```
         B1     B2     B3     B4     B5     B6     B7
u=0.00 [0.167  0.667  0.167  0.000  0.000  0.000  0.000]  ← 行和=1
u=0.14 [0.048  0.591  0.348  0.013  0.000  0.000  0.000]  ← 行和=1
u=0.43 [0.000  0.109  0.622  0.269  0.000  0.000  0.000]  ← 行和=1
u=0.57 [0.000  0.000  0.269  0.622  0.109  0.000  0.000]  ← 行和=1
u=1.00 [0.000  0.000  0.000  0.167  0.667  0.167  0.000]  ← 行和=1
         ↑ 每行最多 degree+1=4 个非零值（局部支撑性）
```

**两个关键性质**：
- **行和恒为 1**（单位分解性）：任意 u 处所有基函数值之和 = 1
- **带状稀疏**：每行最多 `degree+1` 个非零值

### 3.3 基函数的计算方式

底层调用 GNU Scientific Library（GSL）的 `gsl_bspline_deriv_eval`，使用 **de Boor 递推**：

**节点向量构造**（两端重复 `degree` 次，夹持 B 样条）：

```
degree=3, NBreak=5, 断点={0, 0.25, 0.5, 0.75, 1.0}
节点向量: [0, 0, 0, 0,  0.25,  0.5,  0.75,  1, 1, 1, 1]
           ↑__________↑  ←内部断点→        ↑___________↑
           端点重复 3 次                    端点重复 3 次
```

**de Boor 递推**（从阶数 1 逐步构建到目标阶数）：

```
阶数 1:  B_{i,1}(u) = 1 若 t[i] ≤ u < t[i+1]，否则为 0   （矩形，不连续）
阶数 d:  B_{i,d}(u) = α_i · B_{i,d-1}(u) + (1-α_{i+1}) · B_{i+1,d-1}(u)
         其中 α_i = (u - t[i]) / (t[i+d-1] - t[i])       （线性混合权重）

递推 3 次后得到三次 B 样条（C2 连续，每点仅 4 个非零基函数）
```

**导数公式**（递推中同步计算，非数值差分）：

```
B'_{i,d}(u) = (d-1) × [ B_{i,d-1}(u)/(t[i+d-1]-t[i]) - B_{i+1,d-1}(u)/(t[i+d]-t[i+1]) ]
```

一次 `gsl_bspline_deriv_eval(x, nderiv=3, dB)` 调用同时输出 0/1/2/3 阶导数，对应：

| 输出 | 含义 | 用途 |
|------|------|------|
| `BasisVal`    | `B_j(u)`     | 速度约束、目标函数 |
| `BasisValD`   | `B'_j(u)`    | 加速度约束中的 udd 项 |
| `BasisValDD`  | `B''_j(u)`   | 跃度约束（jmax） |
| `BasisValDDD` | `B'''_j(u)`  | 备用 |

---

## 4. 工作变量详解

### 4.1 变量总览

```matlab
at_norm  = zeros(2, N, Nwindow);    % [起/末端 × N × Nwindow] 切向加速度约束行
t_vec    = zeros(Ndim, 2, Nwindow); % [Ndim × 起/末端 × Nwindow] 切线单位向量
v2_vec   = zeros(2, N, Nwindow);    % [起/末端 × N × Nwindow] 速度²约束行
Acc      = zeros(M*Ndim, N, 2);     % [M·Ndim × N × 用途] 加速度约束矩阵
```

三个变量（`at_norm`、`t_vec`、`v2_vec`）的第三维都是 `Nwindow`（段数），
第二维（2）代表**端点**：索引 1 = 起端（u=0），索引 2 = 末端（u=1）。

### 4.2 `t_vec` — 切线单位向量

```matlab
normR1D         = vecnorm(r1D);                          % 各点 |r'(u)|，形状 [1×M]
t_vec(:, :, k)  = r1D(:, [1,end]) ./ normR1D([1, end]);
%                  ↑ 在起端(u=0)和末端(u=1)处取归一化切线方向
%                  形状: (Ndim × 2)
```

`t_vec(:, 1, k)` = 段 k 起端的单位切线向量（Ndim 维）
`t_vec(:, 2, k)` = 段 k 末端的单位切线向量（Ndim 维）

### 4.3 `Acc` — 加速度约束矩阵

物理加速度的链式法则：

$$a_j(u) = r''_j(u) \cdot ud^2 + r'_j(u) \cdot udd$$

代入 `ud² = B(u)·x` 和 `udd = w'(u)/2 = B'(u)·x / 2`（由 calcUfromQ 导出的精确关系）：

$$a_j(u) = \underbrace{\left[r''_j(u)\cdot B(u) + \tfrac{1}{2}\,r'_j(u)\cdot B'(u)\right]}_{Acc\text{ 中轴 }j\text{、点 }u\text{ 对应的 }1\times N\text{ 行}} \cdot x_k$$

```matlab
for j = 1 : Ndim
    ind = (1:M) + (j-1)*M;            % 轴 j 在 Acc 中的行区间
    Acc(ind, :, 1) = r2D_a(j,:)' .* BasisVal   + 0.5 * r1D_a(j,:)' .* BasisValD;
    Acc(ind, :, 2) = r2D(j,:)'   .* BasisVal   + 0.5 * r1D(j,:)'   .* BasisValD;
end
```

**第三维两个切片的区别**：

| 切片 | 构建所用导数 | 用途 | 坐标系 |
|------|------------|------|--------|
| `Acc(:,:,1)` | `r1D_a`, `r2D_a` | A 矩阵（不等式：各轴速度/加速度上限） | 5轴时=关节空间；3轴时=笛卡尔 |
| `Acc(:,:,2)` | `r1D`, `r2D`     | Aeq（等式：切向加速度连续性） | 始终为笛卡尔 |

注意：对于 3 轴机器（`TRAFO=false`），`r1D_a = r1D`、`r2D_a = r2D`，两个切片的坐标系相同，区别仅在于用途。

**Acc 的内存排列**（行按轴交织，M 行一组）：

```
Acc 的行:
  行 1   ~  M  : 轴1（x轴）在 M 个采样点处的加速度系数行
  行 M+1 ~ 2M  : 轴2（y轴）在 M 个采样点处的加速度系数行
  ...
  行 (Ndim-1)*M+1 ~ Ndim*M : 轴Ndim在 M 个采样点处的加速度系数行
```

`indAT` 用于从中抽出端点行（仅取 u=0 或 u=1 处）：

```matlab
indAT = (1:Ndim - 1) * M + [1; M]
%        ↑ 轴 j 的起端行=(j-1)*M+1，末端行=j*M
```

### 4.4 `v2_vec` — 速度²约束行

```matlab
v2_vec(:, :, k) = normR1D([1,end]).^2' .* BasisVal([1;end], :)
%                  (2×1)              .* (2×N)  →  (2×N)
```

展开：
- `v2_vec(1, :, k)` = `|r'(0)|² × B(0)` → 乘以 `x_k` 得 `v²(u=0) = |r'(0)|² × ud²(0)`
- `v2_vec(2, :, k)` = `|r'(1)|² × B(1)` → 乘以 `x_k` 得 `v²(u=1) = |r'(1)|² × ud²(1)`

**与 at_norm 的关键区别**：

| 变量 | 乘以 x_k 得到 | 含义 |
|------|--------------|------|
| `v2_vec(e,:,k)` | `v²(u_e)` = **物理速度平方** | 包含 `\|r'\|²` 系数，端点实际速度 |
| `at_norm(e,:,k)` | `a_t(u_e)` = **切向加速度标量** | 包含切线方向投影，端点速率变化率 |

### 4.5 `at_norm` — 切向加速度约束行（核心）

```matlab
at_norm(1, :, k) = t_vec(:,1,k)' * Acc(indAT(1,:), :, 2)
%                  (1 × Ndim)    ×  (Ndim × N)
%                  ════════════════════════════
%                  结果: (1 × N)
```

**物理意义推导**：

```
Acc(indAT(1,j), :, 2)      — 轴 j 在 u=0 处的加速度系数行（1×N）
    乘以 x_k → a_j(u=0)    — 轴 j 在起端的物理加速度

t_vec(:,1,k)'               — 起端单位切线方向 t = r'(0)/|r'(0)|（1×Ndim）

t_vec(:,1,k)' × Acc(...) × x_k
 = Σ_j  t_j(0) × a_j(0)
 = t(0)·a(0)
 = a_tangential(u=0)        ← 切向加速度（速率沿路径的变化率）
```

几何示意：

```
路径方向 ──────────────→  t（切线方向）
                          ↑
                          │  a_t = t·a  ← at_norm × x_k 给出这个标量
          a（全加速度向量）╱
                         ╱
                        ╱ a_n（法向分量，由曲率引起）
```

**为什么第二维是 N**：`at_norm(e,:,k)` 是 LP 约束矩阵 Aeq 中的**一行**，其作用是把 N 维决策变量 `x_k` 线性映射为一个标量（切向加速度）。N 必须与 `x_k` 的维度相同，才能做合法的内积运算。

---

## 5. 不等式约束 A 的组装

每段 `k` 构成 A 的一个块（块对角结构，段间无耦合）：

```
A 的第 k 段块（Nc×M 行 × N 列）：

  行 1    ~  M     :   BasisVal        ≤  f_max'    （速度上限: ud² ≤ f_max）
  行 M+1  ~ 2M     :  -BasisVal        ≤  0         （速度非负: ud² ≥ 0）
  行 2M+1 ~ 2M+M·Ndim:  Acc(:,:,1)    ≤  b_amax    （加速度上限）
  行 ...  ~ end    : -Acc(:,:,1)       ≤  b_amax    （加速度下限）
```

**速度上限 `f_max` 的计算**（逐点取所有约束中最紧的一个）：

```
per-axis:   v_max(j, i) = (vmax_j / |r'_j(u_i)|)²   ← 轴 j 在点 u_i 处的 ud² 上限
feed-rate:  v_max(end,i)= (F / |r'_cart(u_i)|)²     ← G-code F 指令对应的 ud² 上限

f_max(i) = min over j of v_max(j, i)                 ← 取最紧约束
```

---

## 6. 等式约束 Aeq 的组装

Aeq 共 `2×(Nwindow+1)` 行（(Nwindow+1) 个连接点，每个连接点 2 行）：

```
行  1~2  : 窗口起端边界 (v² = v_0², at = at_0)
行  3~4  : 段 1 末端 = 段 2 起端的连续性
行  5~6  : 段 2 末端 = 段 3 起端的连续性
...
行 end-1~end: 窗口末端边界 (v² = v_1², at = at_1)
```

**关键技巧**：`mask_continuity = [+1; +1; -1; -1]`

对同一组行（indAEL），段 k 贡献 `+continuity`，段 k+1 贡献 `-continuity`：

```
Aeq(那一行, 段k列)   += +continuity  （前段末端）
Aeq(那一行, 段k+1列) += -continuity  （后段起端）

最终效果：[前段末端值] - [后段起端值] = 0  ↔  连续性条件
```

---

## 7. 末端斜坡（Ramp）— 抑制过优化

滑动窗口 LP 每次只"提交"第一段的解，末尾段下次会重新优化。若不加限制，末尾段会被充分利用（速度打满），导致新曲线加入时产生急剧减速。

解决方案：对末尾段的约束右端施加斜坡放宽（`VEL_RAMP_OVER_WINDOWS`），
人为给末尾段**预留余量**，使下次优化时不会出现剧烈速度变化。

```
ramp 结构：
  第 1 段（将被提交）: ×1.0     → 约束不变，精确优化
  末尾段            : ×ramp>1  → 约束放宽，允许更高速（为下次窗口留余地）
  中间段            : ×ramp最大值（统一放宽）
```

---

## 8. continuity 输出

函数最后返回的 `continuity` 不是最终结果，而是**供下一窗口使用的边界条件**：

```matlab
continuity = [v2_vec(2,:,1); at_norm(2,:,1)];
%              ↑ 取第 1 段（k=1）的末端
%   FeedratePlanning_LP 读取它，作为下次调用的 v_0/at_0 起始边界
```

这保证了相邻两次窗口求解之间的**物理连续性**（速度平方和切向加速度均连续过渡）。
